# World Weather Map セットアップ手順書

このアプリを使えるようにするための手順を、はじめてプログラムを動かす方にもわかるように説明します。  
順番通りに進めれば、誰でも動かすことができます。

---

## 必要なもの

| 必要なもの | 説明 |
|---|---|
| Mac または Windows PC | どちらでも動きます |
| インターネット接続 | ダウンロードや天気データ取得に必要です |
| メールアドレス | OpenWeatherMap の無料登録に使います |

所要時間：**約15〜20分**（初回のみ）

---

## 全体の流れ

```
① Node.js のインストール
      ↓
② プロジェクトフォルダを配置
      ↓
③ ターミナルでフォルダに移動
      ↓
④ npm install（必要なプログラムを自動取得）
      ↓
⑤ npm start（サーバー起動）
      ↓
⑥ ブラウザで http://localhost:3000 を開く
      ↓
⑦ 画面の指示に従って API キーを設定
      ↓
⑧ 完成！地図画面が表示される
```

---

## 手順① Node.js をインストールする

「Node.js（ノードジェイエス）」とは、このアプリのサーバーを動かすために必要なプログラムです。

### インストール方法

1. ブラウザで [https://nodejs.org](https://nodejs.org) を開く
2. 緑色の **LTS** ボタンをクリックしてダウンロード
   - 「LTS」は安定版という意味です。こちらを選んでください
3. ダウンロードされたファイルを開いてインストール
   - 途中で何か聞かれたら「続ける」「インストール」を選べば大丈夫です

### インストール確認

インストールが終わったら、正しく入ったか確認します。  
次の手順② でターミナルの開き方を説明しますので、そこで確認してください。

---

## 手順② プロジェクトフォルダを配置する

受け取った **OpenWeather** フォルダを、パソコンのわかりやすい場所に置いてください。

> 例：`書類`フォルダの中など

---

## 手順③ ターミナルを開いてフォルダに移動する

「ターミナル（ターミナル.app）」とは、文字を打ってパソコンに命令するためのアプリです。  
難しく聞こえますが、このガイドに書いてある文字をコピーして貼り付けるだけで大丈夫です。

### ターミナルの開き方

**Mac の場合**
- `Launchpad`（ロケットのアイコン）を開いて「ターミナル」と検索してクリック
- または `Spotlight`（虫眼鏡アイコン、または `Command + スペース`）を開いて「ターミナル」と入力して Enter

**VSCode をお使いの場合（Mac / Windows 共通）**
- VSCode の上部メニュー → `ターミナル` → `新しいターミナル`
- 画面の下部にターミナルが開きます

**Windows の場合**
- スタートメニューを開いて「PowerShell」と検索してクリック

### フォルダへの移動

ターミナルが開いたら、OpenWeather フォルダに移動します。  
以下のように入力して Enter を押してください。

**Mac の場合の例：**
```bash
cd /Users/あなたのユーザー名/Documents/OpenWeather
```

> `あなたのユーザー名` の部分は実際のユーザー名に変えてください。  
> ユーザー名がわからない場合はターミナルで `whoami` と入力すると表示されます。

**現在地の確認（ちゃんと移動できたか確認する）**
```bash
pwd
```
`/Users/あなたのユーザー名/Documents/OpenWeather` のように表示されれば成功です。

---

## 手順④ .env ファイルを作成する

プロジェクトフォルダに `.env.example` というファイルがあります。  
これを `.env` という名前でコピーしてください。

**Mac / Linux の場合：**
```bash
cp .env.example .env
```

**Windows の場合：**
```powershell
copy .env.example .env
```

`.env` ファイルが作成されました。中身は後の手順⑦で書き換えます。

---

## 手順⑤ 必要なプログラムをインストールする

以下のコマンドを入力して Enter を押してください。

```bash
npm install
```

「npm（エヌピーエム）」とは、プログラムに必要な部品を自動でダウンロードするツールです。  
しばらく待つと `node_modules` というフォルダが作られて完了です。

> ⏱ 数十秒〜1分程度かかることがあります。完了するまでそのまま待ってください。

---

## 手順⑥ サーバーを起動する

```bash
npm start
```

以下のように表示されれば起動成功です。

```
サーバー起動: http://localhost:3000
```

> ⚠️ このターミナルは起動中、開いたままにしてください。  
> 閉じるとサーバーが止まってアプリが使えなくなります。

---

## 手順⑦ ブラウザで開く

Chrome・Safari・Firefox などのブラウザを開き、アドレスバーに以下を入力して Enter を押してください。

```
http://localhost:3000
```

> 「localhost（ローカルホスト）」とは「このパソコン自身」という意味です。  
> インターネット上には公開されておらず、あなたのパソコンの中だけで動いています。

**セットアップ画面が表示されます。**  
次の手順⑦ に進んでください。

---

## 手順⑧ API キーを取得して設定する

画面に表示される手順に従って、OpenWeatherMap の無料 API キーを取得し `.env` ファイルに設定します。

### API キーとは

天気データを取得するための「合言葉」のようなものです。無料で取得できます。

### 設定の流れ

1. 画面の「ステップ1」の手順で OpenWeatherMap に登録して API キーを取得する
2. プロジェクトフォルダ内の **`.env`** ファイルをテキストエディタで開く
   - `.env` は「ドット env」と読みます
   - ファイルが見えない場合は隠しファイルの表示をオンにしてください
     - Mac：Finder で `Command + Shift + .`（ピリオド）
3. 以下のように書き換えて保存する（`取得したキー` の部分を実際のキーに変える）

```
OPENWEATHER_API_KEY=取得したキーをここに貼り付ける
```

4. サーバーを再起動する（次の手順を参照）

### サーバーの再起動方法

**① サーバーを止める**

`npm start` を実行しているターミナルで `Ctrl + C` を押す。

> 止まらない場合は、**別のターミナルウィンドウ**を開いて以下を実行：
> ```bash
> kill $(lsof -ti :3000)
> ```

**② 再起動する**

```bash
npm start
```

---

## 手順⑨ 地図画面を開く

ブラウザで再度アクセスします。

```
http://localhost:3000
```

🎉 **地図画面が表示されれば完成です！**

---

## 使い方

| 操作 | 説明 |
|---|---|
| 地図をクリック | クリックした地点の天気を表示 |
| 国を選択 | 左上のドロップダウンから国を選ぶと、その国がハイライトされズームイン |
| 都市を選択 | 国を選んだあと、都市一覧から選ぶとその都市の天気を表示 |
| 緯度・経度で指定 | 画面下のパネルに数値を入力して「表示」ボタンを押す |
| 地図の拡大・縮小 | 右下の＋／－ボタン、またはマウスホイール |

---

## サーバーの止め方

アプリを使い終わったらサーバーを止めてください。

`npm start` を実行したターミナルで：
```
Ctrl + C
```

ターミナルが反応しない場合は、別のターミナルで：
```bash
kill $(lsof -ti :3000)
```

---

## 次回以降の起動方法

2回目以降は以下の2つのコマンドだけで起動できます。

```bash
cd /Users/あなたのユーザー名/Documents/OpenWeather
npm start
```

ブラウザで `http://localhost:3000` を開けばすぐに地図画面が表示されます。

---

## うまくいかないときは

### 「npm が見つかりません」と出る

→ 手順① の Node.js インストールが完了していません。もう一度インストールしてください。

### 「EADDRINUSE: address already in use」と出る

→ すでにサーバーが起動中です。以下で止めてから再度 `npm start` してください。
```bash
kill $(lsof -ti :3000)
```

### 「npm start と打っても何も起きない」

→ OpenWeather フォルダに移動できていません。手順③ を見直してください。
```bash
cd /Users/あなたのユーザー名/Documents/OpenWeather
npm start
```

### 天気が表示されない

→ API キーの設定を確認してください。
- `.env` ファイルに `OPENWEATHER_API_KEY=キー` が正しく書かれているか
- 新規登録直後は最大 **2時間** かかる場合があります
